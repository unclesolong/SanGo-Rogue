function notImplemented(name) {
  throw new Error(`${name} API boundary exists, but implementation still lives in main.js.`);
}

export function showScreenView() {
  notImplemented('showScreenView');
}

export function bindScreenNavigation() {
  notImplemented('bindScreenNavigation');
}
