function notImplemented(name) {
  throw new Error(`${name} API boundary exists, but implementation still lives in main.js.`);
}

export function renderBoardView() {
  notImplemented('renderBoardView');
}

export function renderBoardSelection() {
  notImplemented('renderBoardSelection');
}
