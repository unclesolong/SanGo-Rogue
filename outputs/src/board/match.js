function notImplemented(name) {
  throw new Error(`${name} API boundary exists, but implementation still lives in main.js.`);
}

export function findBoardMatches() {
  notImplemented('findBoardMatches');
}

export function groupMatchedCells() {
  notImplemented('groupMatchedCells');
}

export function summarizeMatchGroups() {
  notImplemented('summarizeMatchGroups');
}
